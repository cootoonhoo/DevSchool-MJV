import { BlueTextDirective } from './blue-text.directive';

describe('BlueTextDirective', () => {
  it('should create an instance', () => {
    const directive = new BlueTextDirective();
    expect(directive).toBeTruthy();
  });
});
